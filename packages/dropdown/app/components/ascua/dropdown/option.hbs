<app-dropdown-option
	disabled={{@disabled}}
	filtered={{matches @label @dropdown.search}}
	selected={{@dropdown.includes (await @value)}}
	{{on "click" (fn @dropdown.changed @value)}}
	...attributes>
	{{@label}}
</app-dropdown-option>